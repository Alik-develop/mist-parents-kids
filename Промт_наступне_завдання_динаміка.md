# Промт для наступного кроку — «Міст для Батьків та Дітей»

Робоча папка: `C:\Users\User\Claude\Projects\Сайт Міст для Батьків та Дітей` (підключена).
Сайт у стилі v2, папка `docs/` (GitHub Pages), гілка `main`. Память підхопиться автоматично.

Уже готово: локальне збереження попыток (localStorage `mist-attempts`, схема `{v:1,attempts}`), кабінет на реальних даних, **реальна динаміка між тестами** — тренди + спарклайни в кабінеті (`#dynamika`), блок «Як змінилося з минулого разу» на результаті (`#r-changed`), експорт JSON + очистка історії (приватність підлітка дотримана).
Контекст читати: `docs/assets/app.js` (шар даних `Site.saveAttempt/getAttempts/getLatest/clearAttempts`, `Site.spark`), `docs/kabinet.html`, `docs/opytuvannya.html`, `docs/assets/styles.css`.

## ЗАВДАННЯ — кілька дітей + поріг «варто звернутися» (на вибір/обидва)

(а) **Профілі кількох дітей.** Зараз дані одного підлітка. Розширити схему до `{v:2, kids:[{id,name,attempts:[...]}], activeKidId}` з **міграцією v1→v2** (наявні `attempts` → дитина «Ваша дитина»). UI перемикача дитини в кабінеті (chip «Ваша дитина» + «Додати»). Шар даних лишити єдиним джерелом правди — UI не переписувати, лише функції читання/запису.

(б) **М'який поріг уваги.** Якщо за останніми N спробами головна зона стабільно «важко» або розходження зростає — теплий, нетривожний банер у кабінеті: «можливо, варто поговорити з фахівцем», з кнопкою на `eksperty.html`. Без алармізму, мовою турботи.

(в) **Динаміка по всіх 4 зонах** (не лише головній) — компактний міні-грід спарклайнів у панелі «Динаміка», згорнутий за замовчуванням («показати всі зони»).

## ВИМОГИ
- Без зовнішніх бібліотек, чистий JS у дусі `app.js`. Шар даних — один источник правды, міграція схеми обов'язково неруйнівна.
- Обидві теми (світла «Тепле молоко» / темна) і мобільна адаптація не ламаються.
- Приватність підлітка: теми/зони/розходження — так, дослівні відповіді — ні.

## QA
- Перевірити міграцію v1→v2 на наявних даних; перемикач дитини; банер порога з'являється лише за умовою; динаміка по зонах будується.
- Логічні тести в node + цілісність файлів через Read-інструмент.
- **Важливо про mount:** bash іноді віддає усічені/пошкоджені свіжі файли — читати через Read-інструмент, перед комітом перевіряти цілісність; якщо з bash файл усічений — НЕ комітити з bash, лишити коміт на Windows і дати команди.

Спілкування — російською; тексти сайту — українською.
