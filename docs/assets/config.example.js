/* ===========================================================
   Зразок конфігурації Supabase.
   ЩО ЗРОБИТИ: скопіюй цей файл поруч під назвою  config.js
   і встав свої значення з Supabase → Project Settings → API.

   • SUPABASE_URL    — «Project URL»
   • SUPABASE_ANON_KEY — «anon public» ключ.
     Цей ключ ПУБЛІЧНИЙ — його можна викладати в репозиторій:
     доступ до даних захищає RLS (правила в supabase_schema.sql).
     НІКОЛИ не встав сюди «service_role» ключ.
   =========================================================== */
window.MIST_CONFIG = {
  SUPABASE_URL: 'https://ВАШ-ПРОЕКТ.supabase.co',
  SUPABASE_ANON_KEY: 'ВАШ-ANON-PUBLIC-KEY'
};
